# The printer's Moonraker WebSocket. In the Tauri bundle the webview origin is tauri.localhost,
# so the screen defaults to ws://localhost:7125/websocket (Moonraker on the same host). Set this
# to point at a remote printer instead.
# VITE_MOONRAKER_WS_URL=ws://printer.local:7125/websocket
